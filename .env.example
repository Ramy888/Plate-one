# Copy to .env and fill in. .env is gitignored.

# AssemblyAI. Worker-side only — the app never sees this key; the Worker mints a
# short-lived session token and the client opens the WebSocket with that.
ASSEMBLYAI_API_KEY=

# Google AI Studio key, for reading a photo of a plate. Worker-side only.
GEMINI_API_KEY=
